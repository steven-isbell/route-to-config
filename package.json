{
  "name": "react-route-to-config",
  "version": "0.0.0",
  "description": "Convert a list of routes into a configuration object.",
  "keywords": [
    "react",
    "react router",
    "routing"
  ],
  "main": "./lib/index.js",
  "scripts": {
    "test": "yarn test",
    "format": "prettier --write",
    "build": "tsc && chmod +x lib/cli.js",
    "watch": "tsc -w"
  },
  "bin": {
    "react-route-to-config": "./lib/cli.js"
  },
  "author": "Steven Isbell <steven.isbell18@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/steven-isbell/route-to-config.git"
  },
  "dependencies": {
    "@babel/parser": "^7.5.5",
    "commander": "^2.20.0"
  },
  "devDependencies": {
    "@babel/types": "^7.5.5",
    "@types/node": "^12.6.8",
    "prettier": "^1.18.2"
  }
}
